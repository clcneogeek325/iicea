from django.conf.urls import patterns, include, url

urlpatterns = patterns('apps.home.views',
    url(r'^$', 'view_index', name='vista_principal'),
    url(r'^$', 'view_login', name='vista_loging'),
    url(r'^$', 'view_logout', name='vista_logout'),
)
