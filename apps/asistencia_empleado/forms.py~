from django import forms
from .models import asistencia_empleado

class asistencia_empleadoForm(forms.ModelForm):
	empleados = forms.ModelMultipleChoiceField(queryset=alumno.objects.filter(activo=True),label="Docente")
	class Meta:
		model = asistencia_empleado
		fields = '__all__'
		
