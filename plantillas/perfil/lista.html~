{% extends "base.html" %}

{% block title %}Lista de Perfiles{% endblock %}

{% block content %}

<!-- ===================================== 
	INSTRUCTIONS 
===================================== -->
<h3>Perfiles</h3>
<a href="{% url 'vista_agregar_perfil' %}">
<button class="medium green"><i class="icon-play-circle"></i>Nuevo Perfil</button>
</a>


<ul class="tabs">
<li><a href="#instructions">Lista</a></li>
</ul>
	<div id="instructions" class="tab-content">
	<div class="col_8">
	<h4><i class="icon-wrench"></i> Setup</h4>
		{% if lista %}
		<!-- Table -->
		<table cellspacing="0" cellpadding="0">
		<thead><tr>
				<th>Nombre</th>
			</tr>
		</thead>
		<tbody>
				{% for p in lista %}
				<tr>
				<td>{{p.nombre}}</td>
				<td><a href="{% url  'vista_editar_perfil' p.id %}" ><button class="medium blue"><i class="icon-edit"></i>Editar</button></a></td>
				<td><button class="medium red" onClick="if (confirm('Esta seguro de querer eliminar {{p.nombre}}!')){window.location.href = '{% url  'vista_eliminar_perfil' p.id %}';};"><i class="icon-minus-sign"></i>Dar de baja</button></td>
				</tr>
				<tr>
				</tr>
				{% endfor %}
		</tbody>
		</table>
		 {% else %}
		 <h1>No se han encontrado perfiles</h1>
		{% endif %}
	
	</div>
	<div class="col_1"></div>
	<div class="col_3">
	<h4><i class="icon-globe"></i> Browsers</h4>
	HTML KickStart Tested and working in IE 8+, Safari, Chrome, Firefox, Opera, Safari IOS, Browser and Chrome Android. 
	<h4>Notes</h4>
	Don't forget to use an HTML5 Doctype
	<code>&lt;!DOCTYPE html&gt;</code>
	</div>
	</div>
{% endblock %}
